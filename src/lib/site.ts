/** Canonical production origin — used for sitemap/robots URLs, JSON-LD
 * `url` fields, and `metadataBase` (so relative OG image URLs resolve). */
export const SITE_URL = "https://cursos.unaividal.com";
export const SITE_NAME = "cursos.unaividal.com";
