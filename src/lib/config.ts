// Mirrors openspec/config.yaml `content.udemy_reverify_days` — that file
// is SDD-phase config (human/agent-facing), this is the runtime's own
// copy. Keep both in sync if this changes.
export const UDEMY_REVERIFY_DAYS = 30;
