# better-auth's peerOptional vitest range (^2||^3||^4) conflicts with our
# real vitest ^5 — it's an optional peer for better-auth's own internal
# tooling, not something this app needs from it. legacy-peer-deps lets npm
# resolve past that instead of hard-failing `npm install`/`npm ci`.
legacy-peer-deps=true
